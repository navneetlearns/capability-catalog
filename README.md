# Capability Catalog

Crawl a platform UI (Journey Builder, Campaigns, Segments, Messaging) to build a structured capability inventory.

## Structure
-  - Playwright-based crawler scripts
-  - Page screenshots (gitignored)
-  - Extracted UI element JSON per page (gitignored)
-  - Final capability catalog

## Usage

